# Minecraft Server Setup Configuration
# Copy this file to .env and customize the values
# The .env file is ignored by git for security

# Minecraft version to install (e.g., "1.21.10")
MINECRAFT_VERSION=1.21.10

# Memory in MB to allocate to the Minecraft server (required in non-interactive mode)
# Recommended: Leave at least 400MB for the operating system
MINECRAFT_MEMORY=6144

# Server name (will be used in server.properties)
MINECRAFT_SERVER_NAME=world

# Auto-start server at boot? (y/n)
MINECRAFT_AUTO_START=y

# Daily reboot at 4am? (y/n)
MINECRAFT_DAILY_REBOOT=n

# Auto-update to latest Minecraft version? (y/n)
# If enabled, server will upgrade to newer versions (e.g., 1.21.3 -> 1.21.4)
# If disabled, only updates builds for the current version (safer for production)
MINECRAFT_AUTO_UPDATE_VERSION=n

# Skip low memory warning? (1 to skip, leave empty to show warning)
# MINECRAFT_SKIP_LOW_MEMORY_WARNING=1

# Force non-interactive mode (optional - auto-detected if any variable above is set)
# NON_INTERACTIVE=1
